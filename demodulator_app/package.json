{
  "name": "rlc-demodulator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && cp ../data/am_composite.wav dist/ && cp ../data/spectrum.json dist/",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
